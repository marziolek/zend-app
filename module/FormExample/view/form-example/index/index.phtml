<?php
    $title = 'Reset password';
    $this->headTitle($title);
?>

<h1><?php echo $this->escapeHtml($title); ?></h1>

<?php
    $form->setAttribute('action', $this->url('form-example', array('action' => 'index')));
    echo $this->renderForm($form);
